module.exports = {
    presets: [
      "@babel/preset-env",        // Transpile modern JavaScript features
      "@babel/preset-typescript",  // Add TypeScript support
    ],
    plugins: [
      // Add any additional plugins if needed, like support for class properties, etc.
    ],
  };