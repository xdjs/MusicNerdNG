# Claude's notes

Working notes from Claude — reasoning that shaped a decision, things noticed while building,
open questions that aren't ready to be a spec yet, and dead ends worth remembering so we don't
walk back into them.

Not a duplicate of `MEMORY.md` (engineering state) or `meetings/` (what the team decided). This
is the thinking in between.

One file per topic, `YYYY-MM-DD-topic.md`.
